DONE 
- have a running angular app
- run a test against the code written in the ui
- show the results of the tc in the ui
- write some js in the ui

TODO
- keyboard shortcut to run the tests

- show the tc one at a time, add one when they all pass
- choose a challenge, show only the tcs related to a given challenges
- view all tc from couch
- record solution in couch
- have a real js editor in the browser
- compute points (pass/fail, coverage)
- isolate test execution (make it impossible to mess with the page with code)
- choose a challenge, show only the tcs related to a given challenges
- show version history (on good states only, where all visible tests pass)
- view other people solution
